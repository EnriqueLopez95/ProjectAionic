import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.Agroecologia',
  appName: 'AppAgroecologia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
